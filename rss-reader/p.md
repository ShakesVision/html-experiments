give me index.html with tailwind and js - that works as a material you (with google material icons - no emojis as icons. use google icons). that is an rss/feed reader.
features:
pagination, with numbers and first last buttons. curren tpage highlighted. posts per page should be modifyable.
reader settings, custom font url support (.woff/.ttf) files etc should be applied, ltr/rtl switch, alignment switches , fontsize adjustment (-A [input showing fontsize currently applied in px, editable] +A, and 'render html' toggle - switched off by default.,
special treatment to blogger/blogpost blogs, they should fetch all blog posts if user gives 'blog' address (instead of a rss feed address, that we normall would expect from user) and fetch all until all are fetched and reader shows those posts.
reader settings should be available sticky /toolbar, but we must have immersive mode to get rid of that bar for good reader ux.
all posts titles should be visible as compact no-fluff, least margin/padding numbered list - clickable to open/jump to those posts. maybe as a 'list' option icon in toolbar that opens sidebar (simiarl to how most epub/ereaders do) this way we will need not scroll through post contents to read next title if we want to look for something specific. also - search function in that? and search in posts too if possible. Go.
